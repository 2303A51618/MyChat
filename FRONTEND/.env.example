# Example FRONTEND environment variables for Vite
# Copy this file to .env (or .env.local) in the FRONTEND folder and update values.
# Vite exposes variables prefixed with VITE_ to the client via import.meta.env

# Backend API URL used by the frontend (e.g. http://localhost:5001 or https://api.yourdomain.com)
VITE_API_URL=http://localhost:5001
